There are two memory reads for the second line of foo. One is loading the value of s.y (&heap0) and the other is loading the value of s.y[i]. The other lines were simply grabbing the address of values which doesnt require a memory read or storing which is a memory write.